package main

// We need a command line interface with arguments
// We need a way of generating keys for the server, and read/write it
// We need a TCP listener
// We should print the fingerprint when starting

func main() {
}
